// Import the main About page component from the same directory
import AboutPage from "./AboutPage";

/**
 * About Page Route Component
 * This is the main entry point for the /about route.
 * It serves as a wrapper that renders the AboutPage component.
 *
 */
export default function Page() {
  // Render the AboutPage component
  return <AboutPage />;
}
